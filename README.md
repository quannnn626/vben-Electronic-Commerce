# 电商后台管理系统

## 启动方式

### 1. 数据库

执行 `sql/vben_mall.sql` 初始化数据库。

### 2. 后端

```bash
cd vue-vben-admin-boot
mvn spring-boot:run
```

默认启动在 `http://localhost:9000`。

### 3. 前端

```bash
cd vue-vben-admin
pnpm install
pnpm dev:ele
```

默认启动在 `http://localhost:5777`。
